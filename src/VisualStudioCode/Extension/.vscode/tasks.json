{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "tasks": [
        {
            "type": "npm",
            "script": "compile",
            "label": "run: compile",
            "detail": "node --max_old_space_size=8192 ./node_modules/gulp/bin/gulp.js compile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}